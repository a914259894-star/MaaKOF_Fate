# 接入与测试指南

把战斗逻辑放进 MaaFw 官方模板（MaaPracticeBoilerplate），并完成效果测试的完整流程。

## 0. 两种起点（选一个）

**起点 A（推荐）**：直接用本工程（`maa-fighter/`）——它已经是官方模板结构 + 战斗实现，
跳到「第 4 节 测试」即可。

**起点 B**：你已 clone 官方模板，想把战斗逻辑合进去。先记住官方模板的**真实布局**
（和我方工程不同）：`agent/` 在**项目根**（不在 assets/ 下）、`interface.json` 在 assets/ 下
且 **agent 段默认是注释掉的**、根目录没有 main.py。按下面步骤移植：

```bash
# 在官方模板根目录执行（假设本工程在 ../maa-fighter）
cp ../maa-fighter/for-boilerplate/main.py           ./main.py   # 模板专用版入口（sys.path 指 agent/）
cp ../maa-fighter/calibrate.py                      ./
cp ../maa-fighter/assets/agent/combat_actions.py    agent/
cp ../maa-fighter/assets/agent/combat_recognition.py agent/
cp ../maa-fighter/assets/agent/farm_actions.py      agent/      # register.py import 了它，漏拷必崩
cp ../maa-fighter/assets/agent/register.py          agent/
cp ../maa-fighter/assets/resource/pipeline/combat.json assets/resource/pipeline/
cp -r ../maa-fighter/assets/resource/image/combat   assets/resource/image/     # 可选（模板匹配备用）
```

然后改三处：

1. `agent/main.py`：整份替换为（模板原版 `import my_action`/`import my_reco` 引用
   示例文件，你的项目里没有，不替换必报 ModuleNotFoundError）——
   ```python
   import sys

   from maa.agent.agent_server import AgentServer
   from maa.toolkit import Toolkit

   import register  # 战斗 + 你的流程的动作/识别统一注册（会连带 import combat_* / farm_actions）


   def main():
       Toolkit.init_option("./")

       if len(sys.argv) < 2:
           print("Usage: python main.py <socket_id>")
           print("socket_id is provided by AgentIdentifier.")
           sys.exit(1)

       socket_id = sys.argv[-1]

       register.register_all(AgentServer)  # 必须在 start_up 之前

       AgentServer.start_up(socket_id)
       AgentServer.join()
       AgentServer.shut_down()


   if __name__ == "__main__":
       main()
   ```
   注意：.py 改动后须重启 agent 进程才生效；combat.json 改动则点 Start 即生效。
2. `assets/interface.json`：
   - 把 agent 段的注释去掉（模板默认整段注释）：
     ```json
     "agent": { "child_exec": "python", "child_args": ["./agent/main.py"] },
     ```
   - `task` 数组加两条：
     ```json
     { "name": "战斗·单场（挂机用）", "entry": "Fight_Once" },
     { "name": "战斗·流水线调试（调阈值用）", "entry": "Fight_Loop" }
     ```
   - 可选清理：`controller` 里的"桌面端 Win32"用不上可删；`resource` 留一条即可。
3. （仅当手动跑 MaaPiCli 报 Failed to start agent 时）把 child_args 改成
   `"../agent/main.py"`。原因：MaaPiCli/通用 UI 源码里 agent 子进程的 cwd =
   interface.json 所在目录（assets/），按 `./agent/main.py` 会找 assets/agent/；
   而 VSCode 插件按项目根解析、CI 打包时 assets/ 会被摊平到安装根，两种场景
   `./agent/main.py` 都没问题——所以先不改，出问题再改。

> 模板自带 `assets/resource/image/empty.png`（image/ 目录已存在 ✓）、
> `assets/resource/model/`（放 OCR 模型用，.gitignore 已忽略 ocr/）。
> `my_action.py / my_reco.py` 可留可删，只要别和 register.py 的注册名冲突。

## 1. 最终文件夹结构

（起点 A 本工程的结构如下；起点 B 官方模板的结构差异见括号内说明）

```
项目根/
├── main.py                              # [嵌入模式] 直接跑；官方模板默认没有，从本工程复制
│                                        #   （模板版: for-boilerplate/main.py；差异仅 sys.path 指 agent/）
├── calibrate.py                         # [校准工具] 亮暗阈值校准
├── agent/                               # ★官方模板: agent/ 在项目根；本工程在 assets/agent/，二选一
│   ├── main.py                          # [标准文件] Agent 模式入口，加 register 两行（见第0节）
│   ├── register.py                      # [战斗] 动作/识别统一注册（加动作只改这里）
│   ├── combat_actions.py                # [战斗·核心] 战斗主循环/普攻/摇杆/蓄力键
│   ├── combat_recognition.py            # [战斗·核心] SkillReadyBright 亮暗判定
│   ├── farm_actions.py                  # [你的流程] FarmTap 通用点按模板（register.py 会 import，不能漏）
│   └── my_action.py / my_reco.py        # 官方模板原文件，可留可删
├── assets/
│   ├── interface.json                   # [标准文件] agent 段取消注释 + task 加 Fight_* 入口（见第0节）
│   └── resource/
│       ├── pipeline/
│       │   ├── combat.json              # [战斗] Fight_* 10 个节点
│       │   └── farm.json                # [你的流程] 自己写（examples/ 有骨架）
│       ├── image/
│       │   ├── combat/                  # [备用] 5 张就绪态模板图（亮暗判定不需要）
│       │   └── farm/                    # [你的流程] 进本/结算按钮模板图
│       └── model/
│           └── ocr/                     # [需下载] det.onnx / rec.onnx / keys.txt（模板 .gitignore 已忽略）
├── examples/
│   ├── farm.sample.json                 # [你的流程] 接线骨架 → 改好复制成 pipeline/farm.json
│   └── farm.sample.说明.md
├── config/                              # [运行时生成] maa_option.json / maa_pi_config.json 调试选项
└── debug/                               # [运行时生成] maafw.log、识别可视化结果图
```

## 2. 战斗相关文件清单

| 文件 | 放在哪 | 干什么 |
|---|---|---|
| `combat_actions.py` | `assets/agent/` | `CombatFighter` 战斗主循环（方案A核心）、`CombatNormalAttack` 普攻、`CombatMove` 摇杆、`CombatPressKey` 蓄力键；顶部 `BUTTONS`/`JOYSTICK` 坐标常量 |
| `combat_recognition.py` | `assets/agent/` | `SkillReadyBright` 亮暗判定（技能能不能放）、`CombatAlwaysHit` 永真识别（普攻兜底用） |
| `register.py` | `assets/agent/` | 把上面这些按名字注册进 Resource（嵌入模式）或 AgentServer（Agent 模式） |
| `combat.json` | `assets/resource/pipeline/` | 战斗流水线：`Fight_Once`（方案A入口）、`Fight_Loop`（方案B调试入口）、7 个技能判定节点（三技能/大招/爆气/秘笈/替身）、普攻、胜负识别。**roi/threshold 都在这改** |
| `combat/*.png` | `assets/resource/image/` | 5 张技能就绪态模板图。**亮暗判定模式下用不到**，只在改回模板匹配时需要 |
| `main.py` / `calibrate.py` | 项目根 | 嵌入模式入口 / 亮暗阈值校准工具 |

## 3. 你需要自己补的内容（用官方工具获取）

官方工具：**VSCode 插件 nekosu.maa-support**（截图+裁剪模板+单节点调试+自动拉
agent，首选）、**MaaDebugger**（网页调试器）、**MFA 工具箱**。
这些工具截的图都按 `interface.json` 的 `display_short_side: 720` 自动缩放到基准尺寸，
量出来的坐标**直接就是**要填的 1280 基准坐标，不用再换算。

| # | 内容 | 用什么拿 | 放到/改到哪 |
|---|---|---|---|
| 1 | ~~各按钮中心坐标~~ **✅ 已完成**（MFATools 实测，见下表，已写入工程） | MFATools | `combat_actions.py` 顶部 `BUTTONS`/`JOYSTICK` + `combat.json` 各节点 `roi` |
| 2 | 技能「就绪 vs 冷却」两态截图各一张 | 战斗中技能亮时截一张、冷却时截一张 | 存任意位置，喂给 calibrate.py |
| 3 | ~~每技能的 threshold~~ **✅ 已定稿**（7 张状态图量化校准，见 3.2；重校才用 calibrate.py） | calibrate.py 输出对比两态数值 | `combat.json` 对应节点的 `custom_recognition_param.threshold`（roi 已用实测框，如两态分不开再调 roi） |
| 4 | （可选）就绪态模板图 | VSCode 插件/MFA 工具箱裁剪图标 | `assets/resource/image/combat/`，并把该节点 recognition 改回 TemplateMatch |
| 5 | 结算页「胜利/失败」识别 roi | 结算出现时截图，量文字位置 | `combat.json` 的 `Fight_Win`/`Fight_Lose`（现为占位 [340,240,600,200]） |
| 6 | OCR 模型三件套 | MaaCommonAssets/OCR（ppocr_v6）或 install.py 自动配 | `assets/resource/model/ocr/` |
| 7 | 你的 farm 节点 + 模板图 | 自己的截图 | `farm.json` + `image/farm/`（examples/ 有骨架） |

### 3.1 坐标实测结果（MFATools，2026-09，已写入工程）

模拟器 2560x1440/640DPI，MFA 工具按 `display_short_side: 720` 自动缩放，
**量出即基准，无需换算**。四元组按 MFA 工具输出格式 `[x, y, w, h]`（左上角+宽高）
解释，点击点 = 框中心：

| 按钮 | roi [x,y,w,h] | 点击点 (x,y) | combat.json 节点 |
|---|---|---|---|
| 摇杆（半径 78 不变） | [155,519,25,25] | 167,531 | —（JOYSTICK 常量） |
| 普攻 | [1122,617,30,30] | 1137,632 | Fight_NormalAttack |
| 一技能 | [964,626,35,34] | 981,643 | Fight_Skill1 |
| 二技能 | [1030,495,30,33] | 1045,511 | Fight_Skill2 |
| 三技能 ★新 | [1155,433,27,29] | 1168,447 | Fight_Skill3 |
| 大招 | [1198,310,28,31] | 1212,325 | Fight_Ult |
| 爆气 | [1041,301,32,36] | 1057,319 | Fight_Burst |
| 秘笈 | [1213,216,27,28] | 1226,229 | Fight_Secret |
| 翻滚/替身 | [1220,544,25,25] | 1232,556 | Fight_Substitute（受击白光环态；普通翻滚不自动放） |

本工程当前状态（同步到你的模板项目 = 按第 0 节 cp 清单整包拷贝即可）：
- `combat_actions.py`：BUTTONS/JOYSTICK 为 MFATools 实测值；含 `skill3`、`roll`、`substitute` 键
- `combat.json`：12 节点；roi 全实测框；新增 `Fight_Skill3`（skills 第 5 位）和
  `Fight_Substitute`（替身，**skills 优先级最前**，只在受击+能量足的白光环态命中；
  普通翻滚不会自动放）；threshold 全部按 7 张真机状态图定稿（见 3.2）；
  `Fight_Burst` 无 target_offset（roi 即按钮框，中心=点击点）；Fight_Loop 的 next 含全部战斗节点
- 12 节点已沙箱验证全部真实加载（post_bundle True + get_node_data 12/12），
  7 节点 × 7 状态判定矩阵 49/49 符合预期
- 验证小提示：若你的四元组其实是"中心 x, 中心 y, 宽, 高"而非左上角，点击点会偏差
  ≤17px（约半个按钮，通常仍能点上）。用 `python main.py --shot shot.png` 对照：
  画一下上表点击点，落在按钮正中即解释正确

**关键提醒**：threshold 已按 2026-09 的 7 张真机状态图全量定稿（见 3.2），可直接开测。
换角色 / 换设备 / UI 布局变化时才需要重校（calibrate.py 两态对比）。

### 3.2 技能状态机与判定定稿（基于 7 张真机状态图，2026-09）

7 张状态图（就绪/全冷却/能量缺/爆气缺/爆气开/受击能量足/受击能量缺）对每个按钮
量化后得到各按钮的**完整状态机**。关键发现：**能量条件已经编码在按钮外观里**——
大招能量不足时图标灰化（暗）、爆气能量不足时按钮整个消失（露出背景）、替身能量
不足时翻滚按钮变暗——所以不需要识别能量条，按钮状态本身就涵盖了能量约束。

| 节点 | 判定方案 | 就绪态 | 不可用态 | 阈值 | 验证矩阵 |
|---|---|---|---|---|---|
| Fight_Skill1 | ratio V≥160 | 0.92 | 0.16（冷却灰化+倒计时） | **0.5** | 7 态全对 |
| Fight_Skill2 | ratio V≥160 | 0.85 | 0.18 | **0.5** | 7 态全对 |
| Fight_Skill3 | **饱和度 median**（图标本身暗红色！亮暗会反向） | S=255 | S=41（灰化） | **150** | 7 态全对 |
| Fight_Ult | ratio V≥160 | 0.85 | 0.19（冷却） / 0.00（能量缺灰化） | **0.45** | 7 态全对（能量不足自动不放） |
| ~~Fight_Burst~~ | ratio V≥160 + confirm 2 | 0.84 | 0.00~0.04（**按钮直接消失**，爆气期间也消失） | ~~0.4~~ | **已按需求移除出战斗流程**（识别命中但游戏常不响应点击→内置冷却空转）；判定数据保留备查 |
| Fight_Secret | ratio V≥160 | 0.92 | 0.31~0.43（使用后变暗+余量角标） | **0.65** | 7 态全对 |
| Fight_Substitute | ratio **V≥245**（受击白光环） | 受击+能量足 = 0.23 纯白环 | 普通态 0.003 / 受击+能量缺 0.00（变暗） | **0.1** | 7 态全对（只在受击+能量足时 HIT） |

要点：
- **Fight_Substitute（替身）已加进 skills 优先级最前**——受击+能量足时立即翻滚触发替身，
  普通翻滚永远不会自动放（符合"只有受击时且能量条足够时触发"）。触发后光环消失，
  天然不会连续触发；点击点位与翻滚相同（1232,556，BUTTONS 里叫 substitute）。
- **Fight_Skill3 用饱和度不用亮度**：该图标就绪态本身是暗红色（V 仅 25），冷却灰化
  后饱和度从 255 掉到 41——亮度判定会反向，饱和度判定完美分离。
- **爆气已移除**（2026-09，用户实测点击易无响应）：skills 列表、Fight_Loop、
  Fight_Burst 节点均已删除；识别方案数据保留在本表备查，需要时按 3.2 参数随时加回。
- 大招三态（就绪 0.85 / 冷却 0.19 / 能量缺 0.00）互不干扰，一个阈值全搞定。
- 验证方式：工程 `_metric` 真实代码路径对 7 图 × 7 节点跑判定矩阵，49/49 符合预期；
  资源加载 post_bundle True + get_node_data 12/12。

仍然缺失的素材（不影响战斗逻辑测试，但影响完整闭环）：
1. **结算页截图（胜利/失败各一张）**——Fight_Win/Fight_Lose 还是占位 roi
   [340,240,600,200]（训练模式出不了结算，正式关卡截）。
2. （可选）替身触发后 1~2 秒的截图——确认光环消失时机。没有也行：替身后状态即变，
   不会重复触发。

## 4. 测试流程（按顺序过，每步有通过标准）

| 阶段 | 命令/操作 | 通过标准 |
|---|---|---|
| 0. 环境自检 | `pip install maafw opencv-python`；`python main.py`（无设备会提示未发现 ADB 设备） | 无 import 错误 |
| 1. 资源加载 | `python main.py --shot shot.png` | 截图保存成功、无 ERR 日志 |
| 2. 坐标核对 | `python calibrate.py --shot shot.png --draw` → 打开生成的 `shot_checked.png` | 红点全落按钮正中、绿框套住对应图标（已在真机图验证 ✓） |
| 3. 亮暗阈值 | ~~已定稿（3.2）~~ 换角色/换设备时才需要：冷却图/就绪图各一张 → `python calibrate.py --shot 图` | 两态数值差 ≥0.15，threshold 填中间值 |
| 4. 单节点验证 | VSCode 插件调试（见下方「本地调试环境」路线 2）：跑 `Fight_Loop` 任务，开 `save_draw` | 每个技能节点：就绪时识别框命中、冷却时不命中 |
| 5. 单场实战 | 嵌入模式：`python main.py --entry Fight_Once`（先手动进到战斗中） | 能普攻连段；技能亮就放、不亮不放；受击出白环时放替身；结算出现后任务正常结束 |
| 6. 完整循环 | 接好 farm.json 后 `python main.py --entry Farm_Enter` | 进本→战斗→结算→再挑战无限循环，体力不足正常退出 |

测试前置（一次性，2 分钟）：在模板根建 `config/maa_option.json`：

```json
{
    "logging": true,
    "save_draw": true,
    "stdout_level": 7,
    "save_on_error": true
}
```

之后每次运行：控制台打全量日志（stdout_level 7）、`debug/maafw.log` 记录每个节点
的识别/动作、识别可视化图带 roi 框落盘、失败自动存截图。VSCode 插件/MaaDebugger
运行时也会自动生成/更新此文件。

看日志定位问题的快捷方式：
```bash
grep -E "Fight_|Recog|action" debug/maafw.log | tail -50   # 看最近50条战斗相关日志
```

### 4.1 MaaDebugger 单节点验证（字段级操作）

前提：`pip install MaaDebugger`；终端 **cd 到项目根** 再运行 `python -m MaaDebugger`
（浏览器自动开 http://localhost:8011；路径相对项目根解析）。

| 界面位置 | 字段 | 填什么（可复制） | 按钮 |
|---|---|---|---|
| Adb 页 | ADB Path | 你模拟器的 adb.exe，如 `C:/Program Files/Netease/MuMuPlayer-12.0/shell/adb.exe` | |
| | ADB Address | MuMu `127.0.0.1:16384` / 雷电 `127.0.0.1:5555` / 夜神 `127.0.0.1:62001` | |
| | ADB Config | `{}` | 点 **Scan** → Devices 下拉选你的模拟器（自动填全）→ 点 **Connect** |
| Resource | 路径输入 | `./assets/resource`（或绝对路径 `D:/Projects/MaaKOF_Fate/MaaKOF_Fate/assets/resource`） | 点 **Load** |
| Agent | Agent Identifier | 留空 | 点 **Connect** → 输入框自动填出 identifier（首次可能超时失败，正常）→ 复制它 → 开第二个终端执行 `python agent/main.py <identifier>` → 回网页**再点一次 Connect**，状态变绿即通 |
| Run Task | Task Entry | `Fight_Loop`（轮询所有节点）或单个节点名 | **Pipeline Override** 填 `{}`；点 **Start**；**Stop** 停止 |

单节点真·单测（避免 next 链进 Fight_Loop 死循环）——Task Entry 填节点名，
Pipeline Override 填（以 Fight_Burst 为例）：

```json
{"Fight_Burst": {"next": []}}
```

验证矩阵（训练模式里逐个状态切换，每个节点跑一遍看 ✅/❌）：

| 节点 | 应该 ✅ 的状态 | 应该 ❌ 的状态 |
|---|---|---|
| Fight_Skill1 / 2 / 3 | 技能亮 | 用掉进冷却（图标灰化+倒计时数字） |
| Fight_Ult | 能量满且图标亮 | 用掉后 / 能量不足（灰化） |
| Fight_Burst | 按钮可见（能量够） | 爆气期间 / 能量不足（按钮消失） |
| Fight_Secret | 秘笈亮 | 用掉后（变暗+余量角标） |
| Fight_Substitute | 受击出白光环时 | 平时 / 受击但能量不足 |

看结果：主页面板截图实时刷新；识别记录列表里**点任意一条**进入详情页
（/reco/{id}）——显示算法、命中框 draw 图、原始 JSON。SkillReadyBright 会返回
`value`（本次算出的亮度/饱和度值），对照 3.2 的表格即可判断阈值是否要微调。
每次点 Start 都会重新加载资源——改完 combat.json 直接重跑，不用重启调试器。
报"未注册/unknown custom recognition" = Agent 没连上（检查第 4 步、identifier
是否和 agent 进程参数一致）。

### 本地调试环境（不需要 install.py，也不需要 MaaPiCli）


模板里的 `tools/install.py` 是**发布打包**脚本——它组装的 `install/` 是给最终用户
MFAAvalonia 通用 UI 用的布局（还会把 MaaCommonAssets 的 ppocr_v6/small 自动拷到
`assets/resource/model/ocr/`），**不是开发测试的前提**。开发测试走下面三条路：

- **路线 1 · 嵌入模式（最快，今天就能测）**：`pip install maafw` 已自带全部原生库，
  `python main.py --shot / --entry Fight_Once` 直接跑。覆盖阶段 0/1/2/3/5/6。
- **路线 2 · VSCode 插件（官方推荐的单节点调试）**：扩展商店装
  `nekosu.maa-support` → 打开项目 → 侧边栏 MAA 图标连接设备跑任务。插件会
  自动下载调试组件、**自动拉起 agent 并注入 socket id**（模板的
  `maatools.config.mts` 已配好：cwd=项目根、interface=assets/interface.json）。
  可单跑 `Fight_Loop`、逐节点看识别框。覆盖阶段 4。
- **路线 3 · MaaDebugger 网页调试器（可选）**：`pip install MaaDebugger` 后在
  「interface.json + resource/ + agent/ 同目录」的组装目录运行 `MaaDebugger` 命令。
  注意它正在重构中，优先用路线 2。

如果你还是想要 `install/`（比如想用 MFAAvalonia 或顺手配 OCR），本地跑也没问题：

```bash
pip install json-with-comments
# 下载 MaaFramework 主包，解压到 deps/（确保 deps/bin 存在；v5.13.0 为当前最新）
curl -L -o /tmp/maa.zip https://github.com/MaaXYZ/MaaFramework/releases/download/v5.13.0/MAA-win-x86_64-v5.13.0.zip
unzip /tmp/maa.zip -d deps/          # zip 顶层就是 bin/、share/ 等
python tools/install.py v0.0.1 win x86_64
```

**关于 MaaPiCli**：它最近已从 MAA 主包拆分为独立包（`MAA-MaaPiCli-<os>-<arch>`），
但截至目前 MaaFramework release 里还没挂出这个独立包、MaaPiCli 仓库也没有
release——过渡期暂时拿不到，**不必纠结**：调试用路线 2，跑任务用路线 1。
（模板 `install.py` 的产物也明确不含 MaaPiCli。）

调试选项：`config/maa_option.json`（工具或 `Toolkit.init_option` 生成）里打开：
- `"logging": true` → `debug/maafw.log`（看每个节点的识别/动作日志）
- `"save_draw": true` → 保存识别可视化结果图（**每一帧识别框画在图上**，调试神器）
- `"stdout_level": 7` → 控制台打全量日志
- `"save_on_error": true` → 任务失败时自动存当前截图

## 5. 战斗代码逻辑（出现 bug 时按这个找）

### 5.1 整体数据流（方案 A）

```
post_task("Fight_Once")
  → pipeline 节点 Fight_Once: recognition=DirectHit（无条件命中）→ action=Custom
  → 调起 CombatFighter.run(context, argv)
       ┌─ 解析 custom_action_param（skills 优先级 / burst_cd / result_nodes / 节奏参数）
       ├─ [可选] keep_moving: 触点0 按住摇杆跑位
       └─ while 循环（每轮 ≈100ms，直到超时/停止）:
            ① ctrl.post_screencap() → cached_image 拿最新帧
            ② run_recognition("Fight_Win"/"Fight_Lose", 图) 命中 → break（战斗结束）
            ③ 按 skills 顺序: run_recognition(技能节点, 图) 命中 → post_touch 点按钮
               - 爆气节点命中后记 burst_cd 内置冷却，冷却内跳过
            ④ 全没命中 → 普攻兜底（按住 60ms 抬起）
       └─ finally: 抬起所有触点（异常/停止也不残留手指）
  → 返回 True → 节点进入 next: [Fight_Win, Fight_Lose]
  → OCR 识别到"胜利"→ 你的结算节点 / "失败"→ 再挑战节点
```

### 5.2 关键设计点（bug 大多出在这几处）

1. **识别定义在 JSON、执行在 Python**：Python 用 `context.run_recognition("节点名", 图)`
   复用 combat.json 里节点的识别（只识别、不执行动作）。所以"判定错了"改 JSON，
   "点错了"改 `combat_actions.py` 的 BUTTONS。
2. **双触点分离**：contact 0 = 摇杆，contact 1 = 普攻/技能。互不干扰，这是
   多点触控正确性的关键；模拟器不支持多触时关掉 `keep_moving`。
3. **一帧一判**：每 100ms 截一帧、7 个技能节点全查一遍（单次判定 ≈0.1ms），
   瓶颈在截图传输（30~100ms）。不会"漏技能"。
4. **爆气冷却**：buff 期间按钮仍亮，靠 `burst_cd` 防重复点；其他技能没有内置
   冷却——如果游戏里某技能点了没放出去且图标仍亮，会被反复点，必要时给该节点
   加 `post_delay` 或单独冷却。
5. **结算后返回**：`Fight_Once` 节点 next 是 [Fight_Win, Fight_Lose]，靠这两个
   OCR 节点把控制权交还你的流程。**Fight_Win/Lose 识别不到 → 任务在战斗节点
   结束后直接终止**（next 全不命中且超时）。
6. **转向逻辑：已按需求移除（2026-09），重新决策时的评估结论备查**——
   - 固定方向轻推（`[1,0]`/`[-1,0]`）：普攻兜底节拍里每 N 秒朝敌方方向推一下摇杆
     （走一步必转身，顺带走近）；最简单，但需要知道敌人大概在哪边
   - 行为反馈摆向（`"auto"`）：技能命中=方向保持；连续 2 周期零命中=换向试探。
     局限：技能全冷却时会误换向（1~2 周期后自纠）
   - probe 边界保护：推杆后测帧差（实测：走动 0.355 / 到边界 0.013，阈值 0.08），
     没走动立即反向——已验证可行，任何方案都可复用
   - 已否决：识别敌人名字/血条位置（固定 UI，无方向信息）；识别脚下蓝圈
     （训练室地板同色域，地面 S=91/V=123 vs 圈 S=91/V=115，颜色不可分）；
     识别脚下朝向箭头（约 15px，模板易碎）
   - 恢复方式：此前完整实现（含 probe）在会话记录里，说一声即可原样恢复

### 5.3 Bug 症状对照表

| 症状 | 最可能的原因 | 排查动作 |
|---|---|---|
| 整场什么都不点 | 控制器输入方式不支持/坐标全错 | `--shot` 核对坐标；换 `input_methods`；看 maafw.log 动作日志 |
| 只普攻、永远不放技能 | 亮暗判定从不命中（threshold 太高 / roi 没贴亮部） | calibrate.py 看两态值；MaaDebugger 看识别框 |
| 技能冷却中也乱放 | threshold 太低；或全屏受击白闪被误判 | threshold 取 B+0.7(A-B)；`confirm: 2` |
| 爆气放了又放 | burst_cd 设短了 | 调大 `custom_action_param.burst_cd` |
| 打了半天任务突然结束 | `duration` 超时兜底（120s）触发，或识别抛异常 | 看 maafw.log；单场耗时超过 120s 就调大 duration |
| 结算页出现还在打 | Fight_Win/Fight_Lose 识别不到（roi 不对/缺 OCR 模型） | 放 OCR 模型或换模板；核对 roi 是否框住结算文字 |
| 战斗结束任务直接终止、不进结算流程 | Fight_Win.next 没接到你的结算节点 | 接线三根线（README 6.1） |
| 停止任务后手指残留 | 不会——finally 里抬手 | 若仍残留，确认控制器支持 post_touch_up |
| 摇杆和攻击"打架" | 模拟器多触点支持差 | `keep_moving: false` |
| 技能点了没放出来 | hold_ms 太短 / 游戏判定未解锁 / 坐标差几像素 | hold_ms 60→100；核对坐标 |
| post_bundle 成功但 get_node_data 找不到节点 | pipeline 目录层级放错 | 对照第 1 节结构；pipeline 顶层不能有注释键 |
